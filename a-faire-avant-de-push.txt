retirer le champ d'ajout d'user dans le dock
retirer la route et le call axios getToken
retirer la route et le call axios deleteAllUsers
retirer l'export et l'import de AuthService dans auth.module et users.module,
  l'injection et le set du cookie token du createUser dans users.controller
changer les CMD des 3 Dockerfile
retirer les volumes dans docker-compose.yml
retirer le gitignore a la racine du repo